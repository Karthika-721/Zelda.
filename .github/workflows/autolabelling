name: Test Auto-Label

on:
  issues:
    types: [opened]

jobs:
  auto-label:
    runs-on: ubuntu-latest
    steps:
      - name: Log Issue Body
        run: echo "Issue Body: ${{ github.event.issue.body }}"
